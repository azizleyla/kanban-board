import{configureStore}from"@reduxjs/toolkit";import cardReducer from"./cardSlice";import{storage}from"redux-persist/lib/storage";const persistConfig={key:"",storage:storage};export const store=configureStore({reducer:{boards:cardReducer}});